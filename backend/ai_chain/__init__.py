"""
AI 链模块 —— 高内聚、低耦合的三层架构

┌─────────────────────────────────┐
│         prompts.py              │  ← 第一层：Prompt 模板管理（纯文本）
│         (模板与策略)             │
├─────────────────────────────────┤
│  base.py        driver_*.py    │  ← 第二层：LLM 驱动适配器（接口抽象）
│  (抽象接口)     (具体实现)      │
├─────────────────────────────────┤
│         parser.py               │  ← 第三层：AI 输出解析与结构化
│         (数据解析)              │
└─────────────────────────────────┘

设计目的：社区贡献者可仅替换 driver 接入新模型，无需改动 Prompt 与解析逻辑。
"""
